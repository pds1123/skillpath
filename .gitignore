# Dependencies
node_modules
**/bin/
**/obj/
backend/SkillPath.Api/App_Data/
backend/database/skillpath-data-relations.png
backend/database/skillpath-data-relations.svg

# Build output
dist
dist-ssr
coverage
playwright-report
test-results

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local

# ─── Private data — NEVER commit ─────────────────────────────────────────────
# One place for local-only source material, personal notes, imports, and exports.
/.local/

# The real question banks, interactive data, image manifest, and question
# images are kept locally only. Public repo uses the *.example.ts stubs.
src/data/questions.ts
src/data/questions_aws.ts
src/data/interactiveData.ts
src/data/questionImages.ts
public/qimages/
content/ctfl-visual-questions.json

# PDFs & extraction artefacts (may contain full question data — never commit)
*.pdf
/tmp/
scripts/*.output
scripts/*_raw.txt
scripts/*_raw.json
scripts/*_checkpoint.json
skills-lock.json

# Env
.env
.env.local
.env.*.local

# ─── Tool / editor state — user-specific, do not commit ─────────────────────
.claude/
.agents/
.impeccable/
.cursor/
.aider*
CLAUDE.md
AGENTS.md
